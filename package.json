{
    "name": "ozon-orders-copier",
    "private": true,
    "scripts": {
        "pretest": "npm run build",
        "test": "node --test \"tests/*.test.js\"",
        "build": "node build/build.js"
    }
}
